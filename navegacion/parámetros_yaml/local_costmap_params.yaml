local_costmap:
  global_frame: /odom                   # marco de referencia al cual opera local_costmap
  robot_base_frame: /base_link          # nombre del marco para la base del robot
  update_frequency: 1.0 #1.0 same global               # frecuencia de actualización del mapa local en Hz
  publish_frequency: 0.5 #0.5 same global              # frecuencia en Hz a la cual el mapa es publicado
  static_map: false #debe ser false     # no se utiliza un mapa estático
  rolling_window: true                  # si el mapa es estático, es falso
  width: 6.0                            # ancho del mapa en metros
  height: 6.0                           # alto del mapa en metros
  resolution: 0.05                      # resolución del mapa en metros/celda
  transform_tolerance: 0.5 # same global   # tolerancia en segundos
  #origin_x -5                          # no necesario dado origen declarado
